import s from "./TryAgain.module.css"

function TryAgain() {
    // const data = props.data
    // console.log(data.login)

    // if(data.login !== '') window.location.replace("/startpage")
    return (
        <div className={s.TryAgain}>
            TRY AGAIN
        </div>
    );
}

export default TryAgain;